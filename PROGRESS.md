# PROGRESS.md — Kronolojik İlerleme Logu

Format: `- [faz/görev] (sahip) ne yapıldı — kararlar/testler`

## Faz 0 — Scaffold
- [F0.1] (Opus) Dizin iskeleti + tracking dosyaları (ARCHITECTURE, DECISIONS, TODO, PROGRESS, STATUS, MANUAL_ACTIONS) oluşturuldu. Mimari + kritik kararlar (PII, veri modeli, güvenlik) belgelendi.
- [F0.2/F0.3] (Opus) Scaffold doğrulandı, `npm install` tamam, ilk commit `2a07cf2` atıldı.
- [F1a] (Opus) `0001_init.sql`: 8 tablo + enum'lar. Onay kapısı DB trigger'ı (`enforce_draft_approval_gate`), `purge_expired_data()` (GDPR Art.17), RLS hazırlığı, `updated_at` otomasyonu.
- [F1d] (Opus) Config modülü: Zod fail-fast doğrulama; ÜRETİMDE mock/dev kaçış yolları (LLM_MOCK, memory DB, dev PII anahtarı) şema seviyesinde reddediliyor.
- [F1c] (Opus) **PII moat tamamlandı.** Deterministik tokenizasyon (bilinen-değer + 15 yapısal desen), AES-256-GCM vault (AAD bağlama ile confused-deputy savunması). IBAN mod-97 ve Steuer-ID yapısal doğrulaması ile yanlış pozitif elemesi. Token enjeksiyonu savunması. **54/54 test geçti** — round-trip ve "sızıntı yok" invaryantları dâhil.
- [F1c-fix] (Opus) Test, Türkçe noktasız `ı`/`I` case-folding kaçağını yakaladı (D-011); düzeltildi + regresyon testi eklendi. Hedef kitle Türk göçmenler olduğu için sessiz bir PII sızıntısı olurdu.
- [F1g] (Opus) `src/common/types/domain.ts` — paylaşılan sözleşmeler paralel agent'lar başlamadan sabitlendi (D-012).
- [F2.0] (Opus) Almanca tarih ayrıştırma + deadline'a göre risk yükseltme + hatırlatma zamanlaması. **30/30 test geçti.** Risk yalnızca yükseltilir, asla düşürülmez.
- [F1b/F1e/F1f/F2.3] (Opus) 4 Sonnet subagent paralel başlatıldı: persistence, LLM servis, kanal adaptörleri, sentetik fixture'lar. Dosya sahiplikleri ayrık.

## Faz 1 — Entegrasyon (subagent çıktıları doğrulandı)
- [F1f] (Sonnet) Kanal katmanı: `ChannelAdapter` soyutlaması, grammY Telegram adaptörü, mock adapter, 3 dilde AI şeffaflık metni, 4096 karakter bölme, onay inline keyboard. 69 test. Ana oturum bağımsız doğruladı: geçti.
- [F2.3] (Sonnet) 8 sentetik Behördenbrief (Ausländerbehörde ×6, Bürgeramt, bilgilendirme) + expected.json + profiles.json. Türk/Suriyeli/Hintli/Somalili/Vietnamlı/Nijeryalı/Ukraynalı/Romen isimleri, 8 farklı şehir.
- [F1b] (Sonnet) Persistence: 8 repository × (memory + supabase), mappers, driver seçimi. 36 test.
- [F1e] (Sonnet) LLM servis: fail-closed sızıntı denetimi, Zod yapılandırılmış çıktı + 1 onarım denemesi, deterministik mock, `OcrProvider` soyutlaması (claude-vision / local tesseract).
- [F1c-fix] (Opus) **D-013 gerçek PII sızıntısı bulundu ve kapatıldı** — fixture tabanlı bağımsız test, aynı Aktenzeichen'in ikinci etiket altında maskesiz kaldığını ortaya çıkardı. Maskeleme iki geçişli hâle getirildi.
- [F1b-fix] (Opus) **D-014 onay kapısı bypass'ı bulundu ve kapatıldı** — tek çağrıda `{status:'sent', approvedAt}` insan onayını atlıyordu; üç katmanda da (memory/supabase/DB trigger) düzeltildi, yanlış davranışı doğrulayan test tersine çevrildi.
- [F1h] (Opus) `app.module.ts` entegrasyonu + `app.module.spec.ts` DI bütünlük testi (anahtarsız boot kanıtı).
- [F1c-fix2] (Opus) **D-015 yüksek etkili recall açığı** — profil yalnızca tam ad içerdiğinde Alman mektuplarının standart hitabındaki ("Sehr geehrter Herr Yılmaz") soyadı maskelenmiyordu. Ad parçaları artık ayrı maskeleniyor.
- [F1e-verify] (Opus) `llm.leak-guard.spec.ts` — 8 gerçek fixture mektubu sahte istemciyle gönderilerek API payload'ında ham PII olmadığı ve sızıntı hâlinde 0 istek gittiği bağımsız olarak kanıtlandı.

## Faz 2 — Çekirdek akış
- [F2.0] (Opus) Deadline/risk yardımcıları — 30 test.
- [F2.1/F2.2] (Opus) **AnalysisPipeline** tamamlandı: ingest → OCR/maskeleme → LLM → vault (şifreli) → deadline token çözümü → risk yükseltme → analiz kaydı → hatırlatma → audit. Uçtan uca test: DB'de ham PII yok, vault round-trip çalışıyor, kullanıcı çıktısı unmask edilmiş, hata yolunda PII loglanmıyor. 298/298 test geçiyor.
- [F3a/F3b/F5.1/F5.3] (Opus) 4 subagent paralel başlatıldı: taslak üretimi, Playwright PoC, hatırlatma+GDPR cron, DevOps.

## Faz 3 & 4 — Üretim, izleme, arayüz
- [F3a] (Sonnet) `DraftsService`: Beamtendeutsch taslak üretimi + tam onay durum makinesi. Vault AAD'ini belgenin GERÇEK sahibine bağlamış (confused-deputy koruması) ve modelin ÜRETTİĞİ metin üzerinde de fail-closed sızıntı denetimi eklemiş — istenmemişti, doğru karar. 9 test.
- [F3b] (Sonnet) `WatcherService` + `AppointmentChecker`: Playwright randevu izleme PoC'si. Gerçek kurum sitesine istek atmaz (etik); yerel mock HTML sayfalarıyla çalışır. Tarayıcı kurulu değilse testler skip edilir, uygulama çökmez. 30 dakikalık bilinçli "kibar polling" aralığı. 10 test + 3 skip.
- [F4.1] (Opus) **`ConversationService` — son halka.** Botu ürün akışına bağlar: /start → AI şeffaflığı → rıza → belge → analiz özeti → /taslak → onay butonları → İNSAN ONAYI → metin kullanıcıya. tr/de/en. 15 test.
  - Kod seviyesinde zorlanan iki kural test edildi: (1) rıza olmadan belge işlenmez — onaysız gönderimde hiç kayıt oluşmaz; (2) hiçbir şey kullanıcı adına kuruma gönderilmez.
- [F1h] (Opus) Tüm modüller `app.module.ts`'e bağlandı; 343 test geçiyor, tsc temiz.

## Faz 5 — Test & Teslim
- [F5.1] (Sonnet) `RemindersService` (saatlik) + `RetentionService` (GDPR Art.17). Silme sırası `purge_expired_data()` ile birebir. Ajan, kapsamı dışında kalan bir eksikliği gizlemek yerine raporladı → D-019.
- [F5.1-fix] (Opus) **D-019 eksik GDPR silme** — `deleteUserData` yalnızca `scheduled` hatırlatmaları siliyordu; `ReminderRepository.findByUser` eklendi (abstract+memory+supabase), üç durumu kapsayan regresyon testi yazıldı.
- [F5.3] (Sonnet) Dockerfile (multi-stage, non-root, healthcheck) + `with-browsers` hedefi, docker-compose, GitHub Actions CI, deployment scriptleri, `docs/DEPLOYMENT.md`. **İmajı gerçekten build etti (218 MB) ve çalıştırdı** — bu sayede iki gerçek hata bulundu.
- [F5.3-fix] (Opus) **D-020 açılışta çökme** — `.env.example` kopyalamak uygulamayı çökertiyordu (boş env değeri Zod `.optional()`'ı geçmiyordu). Merkezî düzeltme + regresyon testi + gerçek boot doğrulaması.
- [F5.3-fix] (Opus) **D-021** — kullanılmayan global `ValidationPipe` kaldırıldı (2 bağımlılık eklemek yerine; uygulamada hiç HTTP controller'ı yok).
- [F5.4] (Opus) README (sorun/çözüm, PII garantisi + dürüst OCR sınırlaması, demo senaryosu) + mermaid diyagramlar.
- [F5.5] (Opus) **DoD doğrulaması tamamlandı.** Playwright tarayıcısı kuruldu; daha önce atlanan 3 test artık gerçekten çalışıyor. **361/361 test geçiyor, 0 atlanan.** MVP bitti.

## Güvenlik Denetimi (MVP sonrası — kullanıcı talebiyle derinlemesine)
- [S-1] (Opus) **Kırmızı takım: onay kapısı.** Kapıyı KIRMAYI deneyen 6 saldırı vektörü yazıldı. Vektör 3 **gerçek bir açık** buldu: `update()` korunuyordu ama `create()` hiç kontrol yapmıyordu — kayıt en baştan `sent` yaratılarak kapı tamamen atlanabiliyordu (memory sürücüsünde; DB trigger'ı zaten reddediyordu). Paylaşılan `assertNotBornSent()` ile üç katman eşitlendi → **D-022**. `approval-gate.redteam.spec.ts` kalıcı.
- [S-2] (Opus) **Sızıntı kanalı denetimi.** Ham PII'nin yalnızca API payload'ından değil, log satırları / DB hata mesajları / exception & stack trace / audit kayıtlarından da sızmadığı doğrulandı (`leak-channels.spec.ts`). Alt katman hatası belge içeriğini yankılasa bile kullanıcıya/DB'ye ulaşmıyor → **D-025**.
- [S-3] (Opus) **D-024 — v1'in gerçek kapsamı ölçüldü.** `PII_PATTERNS` içinde NAME için yapısal desen OLMADIĞI ve v1 akışının profil beslemediği (D-018) ortaya çıktı → kişi adları Claude'a çıplak gidiyordu. `pii.gap-audit.spec.ts` ile sınır sabitlendi.
- [S-4] (Opus) **D-026 — aynı boşluk SAKLAMA tarafını da etkiliyordu:** `documents.masked_text` alanı adına rağmen isimleri ham tutuyordu (GDPR saklama yüzeyi). README/STATUS/ARCHITECTURE'daki "düz PII hiçbir yere yazılmaz" ifadeleri DÜZELTİLDİ.
- [S-5] (Opus) **D-023 — test altyapısı tuzağı:** `ConfigModule.forRoot()` doğrulamayı import anında çalıştırdığı için `beforeEach`'teki env atamaları sessizce yok sayılıyordu. Mevcut testler doğru sonucu şans eseri alıyormuş (istenen değerler zaten varsayılandı).
- [S-6] (Opus) Yarış durumu/eşzamanlılık denetimi: vault'un maskeli metinden ÖNCE yazıldığı (yetim token yok), 8 paralel analiz, kullanıcı izolasyonu ve AAD çapraz-çözme reddi doğrulandı (`pipeline.concurrency.spec.ts`).

## v1.1 — Onboarding, kanal, ölçüm
- [V1.1-a] (Opus) **D-027 Onboarding profili — D-018/D-024 KAPATILDI.** Rıza sonrası 3 adımlı akış (ad → adres → PLZ/şehir). Profil DEĞERLERİ `users` tablosunda değil, `pii_vault`'ta kullanıcı kapsamlı ve **AES-256-GCM şifreli**; `users.profile_completed_at` (migration 0002) yalnızca durum tutar. Kullanıcı `/atla` diyebilir ve adının maskelenmeyeceği AÇIKÇA bildirilir. `onboarding.e2e.spec.ts` ile kanıtlandı: ad artık ne payload'a gidiyor ne de `masked_text`'te saklanıyor.
- [V1.1-b] (Opus) **D-029 Faz A — üçüncü taraf isimleri.** NER yerine deterministik bağlamsal tetikleyiciler (selamlama, Sachbearbeiter/Ansprechpartner/Rechtsanwalt, imza blokları, adres bloğu, aile bağı). Ölçüm: 8 mektupta **16 NAME eşleşmesinin 16'sı gerçek isim — sıfır yanlış pozitif.** Geliştirme sırasında satır-sonu aşırı yakalama hatası bulundu ve düzeltildi. D-026'daki "kısmi maskeleme" vakası da kapandı.
- [V1.1-c] (Opus) **D-030 Telegram webhook endpoint'i.** Gizli anahtar sabit zamanlı doğrulanıyor; sunucuda sır tanımsızsa **fail-closed 401**; işleme hatasında bile 200 (Telegram sonsuz retry yapmasın); loglarda yalnızca `update_id`. Açılışta `setWebhook` ile otomatik kayıt.
- [V1.1-d] (Opus) **D-031 Prompt ölçüm altyapısı** (`npm run eval:prompts`): 8 fixture × gerçek model, alan bazında doğruluk + PII sızıntı raporu, `--out` ile öncesi/sonrası karşılaştırması.

## v1.1 — Gerçek API ile ölçüm (ANTHROPIC_API_KEY sağlandı)
- [M-1] (Opus) İlk gerçek koşum: authority 8/8 · deadline 8/8 · riskLevel 8/8 · missingDocs %100 · **PII sızıntısı YOK**. Token sözleşmesi gerçek modelle uçtan uca doğrulandı.
- [M-2] (Opus) **D-033 — ölçüm aracının KENDİSİ hatalıydı.** `authority` 7/8 görünüyordu; "hata" aslında modelin maskeleme sözleşmesine DOĞRU uymasıydı (`Bürgeramt [[ADDRESS_1]]-Mitte`). Karşılaştırma unmask edilmeden yapılıyordu → düzeltildi, 8/8. Güvenilseydi var olmayan bir sorun için prompt bozulacaktı.
- [M-3] (Opus) **D-032 — `.env` testleri kırdı:** gerçek anahtar eklenince 24 test kırıldı, suite 7s→52s çıktı (testler gerçek API'ye çıkıyordu). `ignoreEnvFile: NODE_ENV==='test'` ile hermetik koşum sağlandı.
- [M-4] (Opus) **riskLevel rubric hipotezi ÇÜRÜTÜLDÜ.** Tavan etkisini kırmak için rubric'in ayrımlarını hedefleyen **6 sınır vakası** (fixture 09-14) üretildi; her biri naif okumanın yanılacağı bir tuzak. Sonuç: rubric YOK 6/6 · rubric VAR 6/6, **0 vaka farkı**; iki rubric'siz koşum arasında da 0 fark. Model tuzakların hepsini rubric olmadan bildi.
- [M-5] (Opus) **Rubric KALDIRILDI** (kullanıcı kararı): ölçülen faydası olmayan metin her çağrıda taşınmaz. Sistem promptu 990 → 723 token (**~267 token/çağrı, %27**). 6 sınır fixture'ı regresyonu korumaya devam ediyor; karar geri alınabilir.

## v1.2 — Supabase canlıya alındı
- [DB-1] (Opus) `scripts/check-supabase.ts` (`npm run check:supabase`): salt-okunur teşhis — proje erişilebilirliği, **anahtar TÜRÜ** (publishable vs secret) ve şema durumu.
- [DB-2] (Opus) İlk denetimde paylaşılan anahtarın **publishable/anon** olduğu tespit edildi. `SERVICE_ROLE` alanına yazılması sessiz bir güvenlik yanılgısı olurdu (politikasız RLS her sorguyu reddeder) → `SUPABASE_ANON_KEY` alanı eklendi ve anahtar TÜRÜNE uygun yere yazıldı. Migration'ın API ile uygulanamayacağı (DDL ⇒ doğrudan Postgres) açıkça raporlandı.
- [DB-3] (Kullanıcı) `service_role` anahtarı sağlandı; `0001_init.sql` + `0002_onboarding_profile.sql` SQL Editor'dan uygulandı → **8/8 tablo**.
- [DB-4] (Opus) **`DB_DRIVER=supabase` yapıldı ve gerçek DB'ye karşı entegrasyon test paketi yazıldı** (`npm run test:supabase`) → **16/16**. Kanıtlananlar: mapper'lar (snake_case↔camelCase, timestamptz/date→`Date`, jsonb iç içe yapı, **numeric(3,2)→number**, 4 enum), `profile_completed_at` (0002), ve **onay kapısı Postgres trigger'ı 7 senaryoda** (onaysız sent ❌ · D-014 bypass ❌ · pending_approval→sent ❌ · D-022 doğrudan sent INSERT ❌ · reddedilmiş ❌ · approved→sent ✅ · zaman damgaları trigger ile dolar) + cascade silme.
- [DB-5] (Opus) Test izolasyonu doğrulandı: entegrasyon testleri `RUN_SUPABASE_TESTS=1` olmadan atlanıyor; `.env`'de `DB_DRIVER=supabase` olmasına rağmen normal koşum hermetik (**527 passed, 16 skipped, 6s**).

## v1.3 — Canlı üretim doğrulaması (gerçek Telegram + gerçek Claude + gerçek Supabase)
- [L-1] (Opus) **Anahtar rotasyonu turu.** Kullanıcı eski Supabase legacy JWT'sini ve Anthropic anahtarını iptal etti; ikisi de bağımsız olarak doğrulandı (`HTTP 401`). Eski JWT'nin `pii_vault`'u okuyabildiği, yani iptalden önce canlı bir kimlik bilgisi olduğu tespit edilmişti. Anthropic iptali `.env`'i kırdı (`LLM_MOCK=false` + ölü anahtar = her analiz başarısız); fark edilip mock moda dönüldü.
- [L-2] (Opus) **Tünel kurulumu.** cloudflared ile HTTPS tüneli; ilk adres bağlantı koptuğu için düştü (NXDOMAIN ile teşhis edildi), ikinci adresle bağlanıldı. `PUBLIC_BASE_URL` + `TELEGRAM_MODE=webhook` ayarlanıp webhook Telegram'a kaydedildi. Kullanıcının `nest start --watch` süreci port 3000'i tuttuğu için EADDRINUSE döngüsü oluşmuştu; teşhis edilip kapatıldı.
- [L-3] (Opus) **Tam yığın doğrulaması** (`npm run live:check`, 11/11): gerçek Claude + gerçek Supabase birlikte ilk kez çalıştırıldı. Bu kombinasyon daha önce hiç test edilmemişti (`eval:prompts` gerçek LLM + memory DB; `test:supabase` gerçek DB + mock LLM).
- [L-4] (Kullanıcı+Opus) **CANLI UÇTAN UCA TEST.** @BuKo749_bot üzerinden: `/start` → AI şeffaflığı → rıza → 3 adımlı onboarding → **metin** mektup analizi ✅ → **fotoğraf** analizi. Fotoğraf ilk denemede BAŞARISIZ oldu.
- [L-5] (Opus) **D-034 — canlı testin bulduğu gerçek üretim hatası.** MIME tipi Telegram'ın `file_path` uzantısından tahmin ediliyordu; uzantı eşleşmeyince `application/octet-stream` üretiliyor ve Claude vision reddediyordu — **yani kullanıcı bota fotoğraf gönderemiyordu.** 555 testin hiçbiri bunu yakalayamazdı (hepsi `MockChannelAdapter` ya da doğrudan metin kullanıyor, gerçek `getFile` yanıtından geçmiyor). Düzeltme: tür artık İÇERİKTEN (sihirli baytlar) tespit ediliyor; uzantı yalnızca yedek. 12 regresyon testi. Ayrıca HEIC (iPhone varsayılanı) Claude tarafından desteklenmediği hâlde "daha net fotoğraf çekin" gibi YANILTICI mesaj veriliyordu → net yönlendirme mesajı eklendi (tr/de/en).
- [L-6] (Opus) Düzeltme sonrası fotoğraf doğrulandı: `image/jpeg` · 160 KB · `analyzed` · *Ausländerbehörde Berlin · Unterlagennachforderung · critical · son tarih 2024-06-30 · güven 0.95 · 4 eksik belge*. **Gizlilik denetimi: `masked_text` + `analyses` + `pii_vault` tarandı → HAM PII YOK** (14 şifreli vault token'ı, 16 yer tutucu).
- [L-7] (Kullanıcı+Opus) **Taslak akışı canlı doğrulandı.** Bir taslak ONAYLANDI (`generated → presented → approved → sent`), farklı bir belge için üretilen ikincisi REDDEDİLDİ (`→ rejected`). Denetim izi eksiksiz ve ham PII içermiyor. **Kapı canlı veriye karşı SALDIRIYLA test edildi:** gerçek reddedilmiş taslakta `reddedilmiş → sent` ❌, `approvedAt` uydurma (D-014) ❌, durum korundu, taslak içeriğinde ham PII yok (27 yer tutucu). "İnsan onayı olmadan hiçbir şey gönderilmez" kuralı artık gerçek Postgres verisinde kanıtlı.
- [L-8] (Opus) **D-035 — PII üretim anahtarı, veri kaybetmeden.** Vault'ta canlı testten kalan **48 şifreli kayıt** vardı (6 profil alanı + 42 belge token'ı), hepsi DEV türetilmiş anahtarla. `PII_MASTER_KEY`'i yalnızca `.env`'de değiştirmek AES-GCM auth tag'ini bozar ve bunları KALICI olarak okunamaz yapardı (profil giderse maskeleme çalışmaz, belge token'ları giderse `masked_text` bir daha çözülemez). Bu yüzden üç fazlı fail-safe rotasyon aracı yazıldı (`npm run rotate:pii-key`): çöz → yazmadan önce round-trip doğrula → yaz. Varsayılan kuru koşum. Sonuç: 48/48 çözüldü, 48/48 doğrulandı, 48/48 yazıldı, `key_version` 1→2. Rotasyon sonrası profil 6/6 alan ve 3 belgenin 42 token'ı tamamen geri kuruluyor; `DEV-ONLY` uyarısı kayboldu.
- [L-9] (Opus) **D-036 — güvenlik kararları canlı ortamda yeniden doğrulandı.** D-010 (OCR istisnası), D-014/D-022 (onay kapısı) ve D-030 (fail-closed webhook) yalnızca birim testleriyle değil, gerçek Telegram + gerçek Postgres verisi üzerinde sınandı. Bu proje defalarca gösterdi ki testlerin doğruladığı şey ile sahada olan ayrışabiliyor (D-024, D-032, D-034) — bu yüzden en güvenlik-kritik iddialar iddia olmaktan çıkarılıp kanıta dönüştürüldü.
- [L-10] (Opus) Belge tutarlılık denetimi: önceki bir commit'te iç içe heredoc çakışması yüzünden STATUS.md ve TODO.md güncellemeleri **sessizce yazılmamıştı** (commit mesajı doğru, dosya güncellenmemiş). Fark edilip düzeltildi.

## v1.4 — Güvenlik borcu kapatma aracı + Railway dağıtım hazırlığı (2026-07-29)
- [R-1] (Opus) **D-037 — Supabase secret anahtar rotasyon aracı** (`npm run rotate:supabase-key`). Kalan tek güvenlik borcu, sohbet geçmişinde görünen `sb_secret_...` anahtarıydı. Faz sırası D-035'in TERSİ: orada risk veri kaybıydı (yanlış anahtar → AES-GCM auth tag bozulur), burada hizmet kaybı (hatalı anahtar → uygulama açılmaz). Bu yüzden ÖNCE tam doğrulama, SONRA yazma. "Tam doğrulama" salt-okunur değil: `/rest/v1/` ile anahtar TÜRÜ (publishable yapıştırmak en olası insan hatası ve o anahtar bazı salt-okunur probe'ları geçerdi), 8/8 tablo, ve gerçek insert → read-back → delete round-trip'i. `.env` yedeği bilinçli olarak YAZILMIYOR — bir `.env.bak`, canlı bir secret'ın diskteki ikinci kopyası olurdu; atomik tmp+rename (mode 0600) kullanılıyor, hedef satır tam 1 kez bulunmazsa yazma iptal. **Gerçek projeye karşı 4 senaryo koşuldu:** geçerli anahtar ✓ · publishable ✗ · sahte `sb_secret_` ✗ · satır 0/2 kez ✗ — üç ret senaryosunda `.env` md5'i değişmedi, `rotation-probe-*` kaydı DB'de kalmadı. Yazma yolu `ROTATE_ENV_PATH` ile geçici kopya üzerinde ayrıca test edildi. **Yan bulgu:** `rotate:pii-key` üç dokümanda anılıyordu ama `package.json`'da hiç yokmuş; eklendi.
- [D-1] (Opus) **D-038 — Railway dağıtım yolunda ÜÇ sessiz arıza.** Dockerfile/CI hazır sanılıyordu, eksik olan sadece hesap bağlamak. Yol gerçekten incelenince kod tarafında üç ayrı arıza bulundu; üçü de deploy'u "yeşil" gösterip ürünü çalışmaz bırakırdı. **(1)** Hedefsiz `docker build` DAİMA son aşamayı derler ve son aşama `with-browsers` idi; compose ve CI `target: runtime` yazdığı için yerelde hiç görünmüyordu — Railway sessizce ~2 GB / Node 20 Playwright imajını üretecekti. Aşamalar yeniden sıralandı. **(2)** `/health` yoktu; Dockerfile `/`'ı (404 döner) yokluyordu, Railway healthcheck'i dağıtımı unhealthy sayıp yeniden başlatma döngüsüne sokabilirdi. **(3)** `PUBLIC_BASE_URL` tavuk-yumurta sorunu: Railway'de genel adres ancak ilk deploy'dan SONRA bilinir ama webhook kaydı AÇILIŞTA gerekir → ilk deploy `localhost`'a webhook kaydeder ve **bot sessizce hiçbir mesaj almaz**. `RAILWAY_PUBLIC_DOMAIN`'den türetiliyor; açık değer her zaman kazanır (özel alan adı bozulmasın), boş string tuzağı D-020 sırasıyla çözüldü.
- [D-2] (Opus) **`/health` — bilinçli olarak liveness, readiness DEĞİL.** Supabase/Anthropic'e kasıtlı dokunulmuyor: sağlayıcı kesintisi çalışan bir süreci öldürmemeli, yeniden başlatmak dış servisi düzeltmez. Ayrıca **sıfır bilgi sızıntısı** — endpoint kimlik doğrulaması isteyemez (platform probe'unun anahtarı yoktur), yani herkese açık; yanıt yalnızca `{status, uptime}`. Alan listesini TAM eşleştiren bir test var, böylece ileride "debug için" eklenen bir alan testi kırar.
- [D-3] (Opus) **`railway.json` + `npm run check:deploy`.** `numReplicas: 1` bilinçli: cron süreç içinde çalışıyor, ikinci replika aynı hatırlatmayı kullanıcıya İKİ KEZ gönderir ve GDPR silmesini çakıştırır. `check:deploy`, `check-env.sh`'den farklı olarak gerçek `validateEnv()`'i çağırır (kural kopyası yok) ve süreç ORTAMINI okur — `railway run npm run check:deploy` ile platformdaki gerçek değişken seti denetlenebilir. Şemanın göremediği tuzakları yakalar: yerel/http `PUBLIC_BASE_URL`, webhook modunda eksik/geçersiz sır (D-030 fail-closed → bot sağır olur), publishable Supabase anahtarı, geçersiz `LLM_MODEL`. Token harcamaz. NO-GO senaryoları tek tek tetiklenerek doğrulandı.
- [D-4] (Opus) **Gerçek Docker doğrulaması.** Hedefsiz build → 218 MB / Node 22 · `NODE_ENV=production` + gerçek `.env` ile konteyner temiz açıldı (0 hata) · `GET /health` → `200 {"status":"ok","uptime":8}` · `/` → 404 · Docker HEALTHCHECK → `healthy`.
- [D-5] (Opus) **D-039 — CI artık hedefsiz build ediyor (regresyon guard'ı).** D-038'in düzeltmesini koruyan hiçbir şey yoktu: CI `target: runtime` yazdığı için sıra tekrar bozulsa CI yeşil kalır, Railway yanlış imajı üretirdi. CI artık Railway ile AYNI yolu izliyor ve imajın gerçekten `runtime` olduğunu kanıtlıyor (Node 22 + Alpine + tarayıcı binary'si yok). **Guard'ın ilk kurbanı kendi varsayımım oldu:** ilk kontrol "`node_modules/playwright` bulunmamalı" diyordu, yerelde koşunca KIRILDI — paket imajda gerçekten var (~18 MB, `optionalDependency`, `--omit=dev` optional'ları atmaz); kaçınılan asıl maliyet tarayıcı binary'leri. Doğru ayrımla değiştirildi ve guard iki yönlü doğrulandı: `runtime` → exit 0, `--target with-browsers` → Node 20 yakalandı, **exit 1**.
- [D-6] (Opus) `docs/DEPLOYMENT.md` iki KAPANMIŞ sorunu (D-020 boş `PII_MASTER_KEY` çöküşü, D-021 eksik `class-validator`) hâlâ açık gibi anlatıyordu — dağıtımı yapan kişiyi yanlış yönlendirirdi. Düzeltildi; Railway bölümü baştan yazıldı, `MANUAL_ACTIONS_REQUIRED.md` §3b (anahtar rotasyonu) ve §8 (Railway) adım adım kopyalanabilir hâle getirildi.
- **Test durumu:** 547 geçiyor (38→40 suite, +8 test), `tsc --noEmit` temiz.

## v1.5 — Supabase anahtar rotasyonu UYGULANDI (2026-07-29)
- [R-2] (Kullanıcı+Opus) **D-040 — güvenlik borcu kapandı.** D-037'de yazılan araç gerçekten uygulandı: yeni secret anahtar (`bukov`) üretildi, `--apply` ile `.env`'e işlendi, sızmış eski anahtar (`bukov2`) Dashboard'dan silindi. **Kritik prosedür kararı:** `--apply` adımı Claude Code oturumunda DEĞİL, kullanıcının kendi terminalinde koşuldu — rotasyonun sebebi eski anahtarın transkriptte görünmesiydi, yenisini buraya yazmak borcu aynı anda yeniden yaratırdı. Bu oturuma yalnızca maskeli parmak izi ulaştı. Araç bunu destekleyecek şekilde tasarlanmıştı: sır içeren adım ile doğrulama adımları ayrı komutlar ve doğrulama adımlarının hiçbiri yeni anahtarın DEĞERİNİ istemiyor.
- [R-2b] (Opus) **Doğrulama turu:** eski anahtar `HTTP 401 "Unregistered API key"` · yeni anahtarla **16/16** gerçek DB entegrasyon testi · `pii_vault` **48/48 kayıt sağlam** (`key_version: 2` — D-035'ten beri değişmemiş; Supabase anahtarı şifreleme yapmadığı için etkilenmemesi gerekiyordu ve etkilenmedi) · 8/8 tablo · `npm run check:deploy` **GO**.
- [R-2c] (Opus) **Sıra hatası ve yayılma gecikmesi kaydedildi.** Eski anahtar revoke edilmeden ÖNCE paylaşıldı; ilk `--check-revoked` `HTTP 200` döndü, yani anahtar kısa süre hem canlı hem transkriptte kaldı. Zarar sınırlıydı (`pii_vault` ayrıca `PII_MASTER_KEY` ile şifreli, o hiç paylaşılmadı). Ayrıca revoke'un yayılması gecikmeli: hemen sonraki kontrol 200, kısa süre sonraki 401 döndü — tek bir "hâlâ canlı" ölçümü kesin kanıt değil, iptal doğrulaması tekrarlanmalı. Doğru sıra MANUAL §3b'ye yazıldı.
- [R-3] (Opus) **Kullanılmayan üçüncü anahtar tespit edildi.** Projede `default` adlı bir secret anahtar daha var. Kod envanteri çıkarıldı: `src/` ve `scripts/` yalnızca `SUPABASE_SERVICE_ROLE_KEY` ve `SUPABASE_ANON_KEY` okuyor. Kullanılmayan ama RLS'i bypass eden kimlik bilgisi saf saldırı yüzeyi → silinmesi önerildi ("Last used" kontrolünden sonra). Değeri kasıtlı olarak İSTENMEDİ.
- [D-6] (Opus) **Railway ön-durumu ölçüldü:** hesap açık ama **0 proje**, GitHub repo'su BOŞ (35 commit push edilmemiş), webhook hâlâ ölü tünelde. Kullanıcı kararı: deploy ertelendi (o sırada anahtar henüz rotate edilmemişti; dağıtım sızmış anahtarı üretime taşırdı). Rotasyon bitti, bu engel kalktı; kalan tek karar GitHub-push mu `railway up` mu.

## v1.6 — ÜRETİME ÇIKILDI (2026-08-11)
- [P-1] (Opus) **GitHub'a push.** Push öncesi sır denetimi: `.env` hiç commit'lenmemiş, 36 commit'lik geçmişin tamamında tam uzunlukta hiçbir sır deseni yok (`sb_secret_`, `sk-ant-`, bot token, 64-hex), depo private. Push sonrası CI otomatik tetiklendi ve **başarılı** — D-039'daki Docker guard'ı ilk kez gerçek GitHub Actions'ta koştu: Node 22 ✓ Alpine ✓ tarayıcı binary'si yok ✓.
- [P-2] (Opus) CI/yerel test sayısı farkı incelendi (544/19 vs 547/16): `appointment-checker.spec.ts` gerçek Chromium istiyor, CI kurmuyor. Tasarlanmış davranış ama randevu izleme PoC'sinin TEK gerçek testi CI'da hiç koşmuyor — bu projede aynı desen iki kez ısırdığı için (D-032, D-034) görünür açık iş olarak kaydedildi.
- [P-3] (Opus) **D-042 — "yeşil deploy, sağır bot".** Railway dağıtımı başarılı görünüyordu (`/health` 200, konteyner ayakta) ama bot hiçbir mesaj alamıyordu. Üç Variables hatası vardı, hiçbiri konteyneri çökertmediği için platform göstergeleri bunları göremiyordu: `TELEGRAM_WEBHOOK_SECRET` **hiç yoktu** (webhook kaydı denenmedi bile), `PUBLIC_BASE_URL=http://localhost:3000` (açık değer kazandığı için D-038'in `RAILWAY_PUBLIC_DOMAIN` otomatiği devreye girmedi), `NODE_ENV=development` (üretim güvenlik kapısı hiç çalışmadı — Dockerfile'daki `ENV NODE_ENV=production` platform Variables tarafından EZİLİYOR). Teşhisi mümkün kılan şey 547 test değil, `railway logs`'taki tek satır oldu: `TELEGRAM_WEBHOOK_SECRET tanımsız — webhook KAYDEDİLMEDİ`. Bu, D-030'un fail-closed tasarımının ikinci faydası: reddetmekle kalmıyor, sebebini loglayıp sessiz başarısızlığı görünür kılıyor.
- [P-4] (Opus) **D-041 — aracın kendisi yanlış GO verdi.** İlk koşuda `railway run npm run check:deploy` "✓ webhook sırrı tanımlı" dedi; oysa Railway'de yoktu. Kök neden: `railway run` platform değişkenlerini enjekte ediyor ama script ayrıca `dotenv` ile yerel `.env`'i yüklüyordu; `dotenv` mevcut `process.env`'i ezmediği için platformda YANLIŞ olanlar doğru görünüyordu (`PUBLIC_BASE_URL` hatası doğru yakalandı) ama EKSİK olanlar sessizce dolduruluyordu. Yani araç, "asıl önemli olan bu" diye belgelenen kullanımda en tehlikeli sınıfa kördü. Düzeltildi: platform ortamı algılanınca `.env` hiç yüklenmiyor, çıktı hangi kaynağı denetlediğini yazıyor. Aynı ortamda önce 1 hata, sonra 2 hata.
- [P-5] (Opus) **Düzeltme + canlı doğrulama.** Üç değişken `railway variables --skip-deploys` ile toplanıp tek `railway redeploy` ile uygulandı; sır `--set-from-stdin` ile geçirildi, değeri komut satırına/transkripte hiç yazılmadı (D-040 kuralı). Sonuç: `BüKo production modunda :8080` · `Telegram webhook kaydedildi: https://b-ko-production.up.railway.app/webhook/telegram` · `check:deploy` **GO (0 hata)** · **D-030 üretimde saldırıyla sınandı: sırsız → 401, yanlış sır → 401** (doğru sırla sahte update kasıtlı olarak gönderilmedi — gerçek bota enjeksiyon olurdu).
- **CANLI:** https://b-ko-production.up.railway.app · @BuKo749_bot
